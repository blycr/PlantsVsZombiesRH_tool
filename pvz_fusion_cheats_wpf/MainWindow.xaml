<Window x:Class="pvz_fusion_cheats_wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PVZ Fusion 修改器 v7" Height="600" Width="680"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">

    <Window.Resources>
        <!-- Custom Toggle Switch Style -->
        <Style x:Key="ToggleSwitchStyle" TargetType="ToggleButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid>
                            <Border x:Name="BackgroundBorder" Width="44" Height="22" CornerRadius="11" Background="#33FFFFFF" BorderThickness="0" Cursor="Hand" SnapsToDevicePixels="True" VerticalAlignment="Center">
                                <Ellipse x:Name="Thumb" Width="18" Height="18" Fill="White" HorizontalAlignment="Left" Margin="2,0,2,0" VerticalAlignment="Center">
                                    <Ellipse.RenderTransform>
                                        <TranslateTransform X="0"/>
                                    </Ellipse.RenderTransform>
                                </Ellipse>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="BackgroundBorder" Property="Background" Value="#FF10B981"/> <!-- Green-500 -->
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Thumb" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" To="22" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Thumb" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" To="0" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BackgroundBorder" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="BackgroundBorder" Property="Background" Value="#11FFFFFF"/>
                                <Setter TargetName="Thumb" Property="Fill" Value="#44FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Accent Button Style -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF3B82F6"/> <!-- Blue-500 -->
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="16,8,16,8"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF2563EB"/> <!-- Blue-600 -->
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#FF1D4ED8"/> <!-- Blue-700 -->
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Modern Danger Button Style -->
        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Background" Value="#FFEF4444"/> <!-- Red-500 -->
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFDC2626"/> <!-- Red-600 -->
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#FFB91C1C"/> <!-- Red-700 -->
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- System Window Buttons (Close/Min) -->
        <Style x:Key="WindowControlButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FFA0AEC0"/>
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1AFFFFFF"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Feature Card Style -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#1A202C"/> <!-- Charcoal Blue -->
            <Setter Property="BorderBrush" Value="#2D3748"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Margin" Value="6"/>
            <Setter Property="Padding" Value="16,12,16,12"/>
        </Style>
    </Window.Resources>

    <!-- Main Container Outer Border (Renders the rounded background shadow) -->
    <Border CornerRadius="16" BorderBrush="#3F4E65" BorderThickness="1.5">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#1E1E2D" Offset="0.0"/>
                <GradientStop Color="#151521" Offset="1.0"/>
            </LinearGradientBrush>
        </Border.Background>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/> <!-- Header Bar -->
                <RowDefinition Height="*"/>  <!-- Main Body -->
                <RowDefinition Height="60"/> <!-- Footer Bar -->
            </Grid.RowDefinitions>

            <!-- 1. Header Bar Area -->
            <Grid Grid.Row="0" Background="#11FFFFFF" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Title & Connection Status -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                    <TextBlock x:Name="Text_Title" Text="PVZ Fusion 3.6.1 修改器" Foreground="White" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                    <TextBlock x:Name="Text_SubTitle" Text="v7 GUI" Foreground="#FF3B82F6" FontSize="12" Margin="6,0,0,0" VerticalAlignment="Bottom" FontWeight="Medium"/>
                    
                    <!-- Connected / Detached State -->
                    <Border x:Name="StatusDot" Width="8" Height="8" CornerRadius="4" Background="#FFEF4444" Margin="20,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="StatusLabel" Text="等待游戏启动..." Foreground="#FFA0AEC0" FontSize="12" Margin="6,0,0,0" VerticalAlignment="Center" FontWeight="Medium"/>
                </StackPanel>

                <!-- Window Control Buttons -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,8,0" VerticalAlignment="Center">
                    <Button x:Name="Btn_Language" Content="EN" Style="{StaticResource WindowControlButtonStyle}" FontSize="12" Click="Language_Click" Margin="0,0,8,0"/>
                    <Button Content="—" Style="{StaticResource WindowControlButtonStyle}" Click="Min_Click"/>
                    <Button Content="✕" Style="{StaticResource WindowControlButtonStyle}" FontSize="14" Click="Close_Click"/>
                </StackPanel>
            </Grid>

            <!-- 2. Main Body Area -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="10,10,10,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/> <!-- Regular Features -->
                        <RowDefinition Height="Auto"/> <!-- Speed feature special card -->
                    </Grid.RowDefinitions>

                    <!-- Features Grid -->
                    <UniformGrid Grid.Row="0" Columns="2">
                        <!-- Feature 1: CD -->
                        <Border Style="{StaticResource CardStyle}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel VerticalAlignment="Center" Margin="0,0,10,0">
                                    <TextBlock x:Name="Text_CooldownTitle" Text="极速冷却 ×100" Foreground="White" FontSize="14" FontWeight="Bold"/>
                                    <TextBlock x:Name="Text_CooldownDesc" Text="卡牌和手套瞬间冷却完毕" Foreground="#FFA0AEC0" FontSize="11" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                </StackPanel>
                                <ToggleButton Grid.Column="1" x:Name="Toggle_Cooldown" Style="{StaticResource ToggleSwitchStyle}" Checked="Feature_Checked" Unchecked="Feature_Unchecked" IsEnabled="False"/>
                            </Grid>
                        </Border>

                        <!-- Feature 2: Sun -->
                        <Border Style="{StaticResource CardStyle}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel VerticalAlignment="Center" Margin="0,0,10,0">
                                    <TextBlock x:Name="Text_SunTitle" Text="阳光越花越多" Foreground="White" FontSize="14" FontWeight="Bold"/>
                                    <TextBlock x:Name="Text_SunDesc" Text="捡阳光或消耗阳光皆100倍增加" Foreground="#FFA0AEC0" FontSize="11" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                </StackPanel>
                                <ToggleButton Grid.Column="1" x:Name="Toggle_Sun" Style="{StaticResource ToggleSwitchStyle}" Checked="Feature_Checked" Unchecked="Feature_Unchecked" IsEnabled="False"/>
                            </Grid>
                        </Border>

                        <!-- Feature 3: Planting -->
                        <Border Style="{StaticResource CardStyle}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel VerticalAlignment="Center" Margin="0,0,10,0">
                                    <TextBlock x:Name="Text_PlacementTitle" Text="任意种植与重叠融合" Foreground="White" FontSize="14" FontWeight="Bold"/>
                                    <TextBlock x:Name="Text_PlacementDesc" Text="打破地形限制重叠种植且可正常融合" Foreground="#FFA0AEC0" FontSize="11" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                </StackPanel>
                                <ToggleButton Grid.Column="1" x:Name="Toggle_Placement" Style="{StaticResource ToggleSwitchStyle}" Checked="Feature_Checked" Unchecked="Feature_Unchecked" IsEnabled="False"/>
                            </Grid>
                        </Border>

                        <!-- Feature 4: Invincible -->
                        <Border Style="{StaticResource CardStyle}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel VerticalAlignment="Center" Margin="0,0,10,0">
                                    <TextBlock x:Name="Text_InvincibleTitle" Text="植物无敌" Foreground="White" FontSize="14" FontWeight="Bold"/>
                                    <TextBlock x:Name="Text_InvincibleDesc" Text="免疫啃食与秒杀，不影响爆炸自毁" Foreground="#FFA0AEC0" FontSize="11" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                </StackPanel>
                                <ToggleButton Grid.Column="1" x:Name="Toggle_Invincible" Style="{StaticResource ToggleSwitchStyle}" Checked="Feature_Checked" Unchecked="Feature_Unchecked" IsEnabled="False"/>
                            </Grid>
                        </Border>

                        <!-- Feature 5: OneHitKill -->
                        <Border Style="{StaticResource CardStyle}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel VerticalAlignment="Center" Margin="0,0,10,0">
                                    <TextBlock x:Name="Text_OneHitKillTitle" Text="僵尸一击必杀" Foreground="White" FontSize="14" FontWeight="Bold"/>
                                    <TextBlock x:Name="Text_OneHitKillDesc" Text="僵尸受到任意微小伤害均会被秒杀" Foreground="#FFA0AEC0" FontSize="11" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                </StackPanel>
                                <ToggleButton Grid.Column="1" x:Name="Toggle_OneHitKill" Style="{StaticResource ToggleSwitchStyle}" Checked="Feature_Checked" Unchecked="Feature_Unchecked" IsEnabled="False"/>
                            </Grid>
                        </Border>

                        <!-- Feature 6: Accelerate -->
                        <Border Style="{StaticResource CardStyle}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel VerticalAlignment="Center" Margin="0,0,10,0">
                                    <TextBlock x:Name="Text_AccelerateTitle" Text="特定植物状态加速" Foreground="White" FontSize="14" FontWeight="Bold"/>
                                    <TextBlock x:Name="Text_AccelerateDesc" Text="大嘴花咀嚼与土豆地雷准备等加速20倍" Foreground="#FFA0AEC0" FontSize="11" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                </StackPanel>
                                <ToggleButton Grid.Column="1" x:Name="Toggle_Accelerate" Style="{StaticResource ToggleSwitchStyle}" Checked="Feature_Checked" Unchecked="Feature_Unchecked" IsEnabled="False"/>
                            </Grid>
                        </Border>
                    </UniformGrid>

                    <!-- Feature 7: Global Speed Game Speed Controller (Horizontal Span) -->
                    <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="6,12,6,12">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Header of speed card -->
                            <Grid Grid.Row="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Margin="0,0,10,0">
                                    <TextBlock x:Name="Text_SpeedTitle" Text="自由调节游戏整体速率" Foreground="White" FontSize="14" FontWeight="Bold"/>
                                    <TextBlock x:Name="Text_SpeedDesc" Text="自由加速/减速游戏全局运行速率 (支持0.1x至10.0x)" Foreground="#FFA0AEC0" FontSize="11" Margin="0,4,0,0"/>
                                </StackPanel>
                                <ToggleButton Grid.Column="1" x:Name="Toggle_Speed" Style="{StaticResource ToggleSwitchStyle}" Checked="SpeedFeature_Checked" Unchecked="SpeedFeature_Unchecked" IsEnabled="False"/>
                            </Grid>
                            
                            <!-- Slider & Text Details (Only active when speed toggle checked) -->
                            <Grid Grid.Row="1" Margin="0,12,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <Slider Grid.Column="0" x:Name="Slider_Speed" Minimum="0.1" Maximum="10.0" Value="1.0" TickFrequency="0.1" IsSnapToTickEnabled="True" VerticalAlignment="Center" Margin="0,0,16,0" ValueChanged="Slider_Speed_ValueChanged" IsEnabled="False"/>
                                
                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Width="80">
                                    <TextBlock x:Name="Text_SpeedValue" Text="1.0" Foreground="#FF10B981" FontSize="15" FontWeight="Bold" Width="40" TextAlignment="Right"/>
                                    <TextBlock x:Name="Text_SpeedUnit" Text="x" Foreground="#FFA0AEC0" FontSize="14" FontWeight="Bold" Margin="2,0,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </Border>
                </Grid>
            </ScrollViewer>

            <!-- 3. Footer Bar Area -->
            <Grid Grid.Row="2" Background="#0AFFFFFF">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Admin Relaunch Button (Only shown if process cannot attach due to permissions) -->
                <Button Grid.Column="0" x:Name="Btn_AdminRelaunch" Content="管理员身份运行" Style="{StaticResource DangerButtonStyle}" Margin="16,0,0,0" VerticalAlignment="Center" Visibility="Collapsed" Click="Btn_AdminRelaunch_Click"/>

                <!-- Action Button Group -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,16,0">
                    <Button x:Name="Btn_EnableAll" Content="一键开启所有" Style="{StaticResource ModernButtonStyle}" Margin="0,0,12,0" Click="Btn_EnableAll_Click"/>
                    <Button x:Name="Btn_RestoreAll" Content="全部还原" Style="{StaticResource DangerButtonStyle}" Click="Btn_RestoreAll_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
